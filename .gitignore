# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# LaTeX build products (docs/)
docs/**/*.aux
docs/**/*.log
docs/**/*.out
docs/**/*.toc
docs/**/*.fls
docs/**/*.fdb_latexmk
docs/**/*.synctex.gz
docs/**/*.lof
docs/**/*.lot

# Isaac Lab run output if launched from here
logs/
outputs/

# Generated by the MJCF -> USD converter from luna_hifi_tasks/excavator/excavator.py.
# assets/config.yaml and assets/.asset_hash ARE tracked and the converter
# rewrites them, so a stash or checkout can revert the bookkeeping while this
# stays behind, or vice versa. scripts/dig_demo.py and scripts/smoke_test.py
# check the asset against its source before spawning anything.
assets/excavator/
